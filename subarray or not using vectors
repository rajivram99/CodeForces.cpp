#include <bits/stdc++.h>

using namespace std;

int main() {
    int a,b;
   vector<int>v1 = {1,2,3,4,5};
   vector<int>v2 = {3,4,5};
   vector<int>::iterator it;
    it =search(v1.begin(), v1.end(), v2.begin(), v2.end());
        if(it!=v1.end())
        {
            cout<<"yes";
        }
        else
        {
            cout<<"NO";
        }
    
cout<<"Nothing";
}
